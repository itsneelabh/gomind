# Required: Redis connection for service discovery
REDIS_URL=redis://localhost:6379

# Optional: HTTP server port (default: 8093)
PORT=8093

# Optional: Kubernetes namespace for discovery
NAMESPACE=default

# AI Provider (one of the following - auto-detected)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GROQ_API_KEY=gsk_...
# GEMINI_API_KEY=...
# DEEPSEEK_API_KEY=...

# Optional: Custom OpenAI-compatible endpoint
# OPENAI_BASE_URL=https://api.example.com/v1

# Development mode (enables verbose logging)
DEV_MODE=false

# Resilience configuration (uses framework defaults if not set)
# These are informational - the framework's DefaultConfig() provides:
#   ErrorThreshold:   0.5 (50% error rate opens circuit)
#   VolumeThreshold:  10  (minimum requests before evaluation)
#   SleepWindow:      30s (wait before half-open)
#   HalfOpenRequests: 5   (test requests in half-open)
#   SuccessThreshold: 0.6 (60% success to close)
#   WindowSize:       60s (sliding window for metrics)

# Retry configuration (uses framework defaults if not set)
# These are informational - the framework's DefaultRetryConfig() provides:
#   MaxAttempts:   3
#   InitialDelay:  100ms
#   MaxDelay:      5s
#   BackoffFactor: 2.0
#   JitterEnabled: true
